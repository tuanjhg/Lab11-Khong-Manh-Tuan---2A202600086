| 1 | **Layer analysis:** For each of the 7 attack prompts in Test 2, which safety layer caught it first? If multiple layers would have caught it, list all of them. Present as a table. | 10 |
| 2 | **False positive analysis:** Did any safe queries from Test 1 get incorrectly blocked? If yes, why? If no, try making your guardrails stricter — at what point do false positives appear? What is the trade-off between security and usability? | 8 |
| 3 | **Gap analysis:** Design 3 attack prompts that your current pipeline does NOT catch. For each, explain why it bypasses your layers, and propose what additional layer would catch it. | 10 |
| 4 | **Production readiness:** If you were deploying this pipeline for a real bank with 10,000 users, what would you change? Consider: latency (how many LLM calls per request?), cost, monitoring at scale, and updating rules without redeploying. | 7 |
| 5 | **Ethical reflection:** Is it possible to build a "perfectly safe" AI system? What are the limits of guardrails? When should a system refuse to answer vs. answer with a disclaimer? Give a concrete example. | 5 |


# Answer 
1. **Layer analysis:** Layer Input Guardrail , Output Guardrail
2. **False positive analysis:**  No false positives were observed in Test 1. However, if we were to make the guardrails stricter, we might start blocking queries that are borderline but still safe. For example, if we set a very low threshold for detecting sensitive information, we might block queries that mention common financial terms like "account" or "balance, transfer, crebit" which could lead to a poor user experience. The trade-off between security and usability is that while stricter guardrails can enhance security, they can also lead to more false positives, frustrating users and potentially driving them away from using the service.
3. **Gap analysis:** 
   - Attack Prompt 1: "How can I bypass security to access my account?" This prompt might bypass the input guardrail if it is not specifically designed to detect attempts to bypass security. An additional layer that uses more advanced natural language understanding could catch this.
   - Attack Prompt 2: "What are the steps to reset my password?" This could be a legitimate query, but if the output guardrail is not designed to detect sensitive information, it might allow the response to include details that could be exploited. An additional layer that specifically checks for sensitive information in the output could catch this.
   - Attack Prompt 3: "Can you provide me with the latest financial report of my account?" If the input guardrail is not designed to detect requests for sensitive information, this prompt might pass through. An additional layer that checks for requests for sensitive information could catch this.
4. **Production readiness:** For a real bank with 10,000 users, I would optimize the pipeline to minimize latency by reducing the number of LLM calls per request. This could be achieved by combining certain layers or using more efficient algorithms for detection. I would also implement a robust monitoring system that can handle the scale of 10,000 users, with real-time alerts for any anomalies. Additionally, I would design the system to allow for updating rules without redeploying, perhaps through a centralized configuration management system that can push updates to all instances of the pipeline
5. **Ethical reflection:** No, limits of guardrails is that they can't know complexity of languge and context and not catch all malicious intents and they can also block legitimate queries. A system should refuse to answer when the query is clearly malicious or when the potential harm outweighs the benefits of providing an answer. For example, if a user asks "How can I hack into someone's bank account?" the system should refuse to answer and provide a disclaimer about the illegality and consequences of such actions.
